const results = URLSearchParams(window.location.search);

const season = results.get('seasons');
switch(seasons){
    case 'summer':
    case 'autumn':
    case 'winter':
                case 'spring': 


    
}